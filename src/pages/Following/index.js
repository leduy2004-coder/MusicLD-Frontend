import RequestFollow from "~/components/RequestFollow";

function Following() {
    return (
        <div style={{padding : 20
        }}>
           <RequestFollow/>
       </div>
    );
    
}

export default Following;